# 2026-03-28

## Raw Events

- Timestamp: 2026-03-28T19:02:00Z
  - Event: Ran hourly OM sweep, refreshed the Codex ledger/status snapshot, and checked `memory/` for stale transcript artifacts.
  - Context: Cron-driven hygiene pass for memory lanes and expenditure tracking.
  - Source: `python3 scripts/token_tracker.py report --date $(date -u +%F) --write`; `python3 scripts/token_tracker.py status --date $(date -u +%F)`; line-count review of `memory/*.md`
  - Findings: Wrote `reports/expenditure/ledger-2026-03-28.md`; active subagents `0`; `MEMORY.md` remains compact at 51 lines. No `memory/` file exceeds 200 lines. The only stale-memory candidates remain `memory/2026-02-23-2227.md` and `memory/2026-03-12-delegation-test.md`, which still look like naming-hygiene artifacts rather than transcript-sized content, so no relocation to `reports/openclaw/` was needed.

- Timestamp: 2026-03-28T19:43:00Z
  - Event: Re-ran the hourly OM sweep and rechecked memory-lane hygiene.
  - Context: Cron-driven verification pass for Codex spend tracking and stale-memory screening.
  - Source: `python3 scripts/token_tracker.py report --date $(date -u +%F) --write && python3 scripts/token_tracker.py status --date $(date -u +%F)`; `find memory -maxdepth 1 -type f | sort | xargs -r wc -l`; inspection of `memory/2026-02-23-2227.md` and `memory/2026-03-12-delegation-test.md`
  - Findings: Refreshed `reports/expenditure/ledger-2026-03-28.md`; Codex daily totals now read 12 turns / 19,983 input / 1,612 output / 74,368 cached / $0.0927; active subagents remain `0`; `MEMORY.md` stays under the compactness ceiling at 51 lines; no `memory/` file exceeds 200 lines. The two stale candidates are still short naming-hygiene artifacts, not oversized transcript dumps, so nothing needed relocation to `reports/openclaw/`.
  - Reflection Candidate: Current stale-memory alerts are still better treated as filename-hygiene checks unless a candidate is also large or transcript-shaped.

- Timestamp: 2026-03-28T21:15:00Z
  - Event: Began Hermes-era Watson migration and validated the local-main / Codex-delegate runtime split.
  - Context: Replaced OpenClaw-first assumptions with Hermes-native routing while keeping `/home/juanbeck/Watson` as the external Watson workspace.
  - Source: `~/.hermes/config.yaml`; `hermes config`; `hermes chat -q "Reply with exactly LOCAL_OK." -m qwen3.5:9b`; `python3 scripts/hermes_usage_report.py status --date 2026-03-28`; `python3 scripts/hermes_usage_report.py report --date 2026-03-28 --write`
  - Findings: Hermes now targets Windows Ollama at `http://172.24.16.1:11434/v1` for the default local main model (`qwen3.5:9b`), while delegation defaults stay on `openai-codex/gpt-5.4`. Added `scripts/hermes_usage_report.py`, wrote a Hermes-native usage section into `reports/expenditure/ledger-2026-03-28.md`, emitted `reports/expenditure/hourly/hermes-usage-2026-03-28-22.json`, and documented the migration plan in `reports/research/hermes-migration-blueprint-2026-03-28.md`.
  - Reflection Candidate: Hermes-native reporting should replace OpenClaw session parsing for ongoing Codex/local usage tracking because Hermes `state.db` already exposes the session totals directly.

- Timestamp: 2026-03-28T22:00:00Z
  - Event: Refactored Watson OM retrieval for Hermes-era operation and added a minimal repo-bound OM hygiene cron.
  - Context: Removed the remaining OpenClaw semantic-search dependency from the retriever while preserving deterministic and lexical repo retrieval.
  - Source: `scripts/om/retriever-worker.sh`; `reports/research/om-hermes-compat-audit-2026-03-28.md`; Hermes cron jobs `watson-om-hygiene` and `watson-hermes-usage-report`; `~/.hermes/sessions/session_20260328_222537_8fc4c5.json`; `~/.hermes/sessions/session_20260328_223140_9fcf97.json`
  - Findings: `retriever-worker.sh` now reports semantic expansion as intentionally disabled/deprecated instead of attempting OpenClaw memory search, `observer-worker.sh` remains salvageable as a repo-bound writer, and the daily template no longer injects placeholder entries into new memory files. Added a 2-hour Hermes cron job (`watson-om-hygiene`) scoped to Watson repo OM maintenance only. Qwen 9B remains usable for simple local orchestration but still emits malformed pseudo-toolcall text in some Hermes tool-use attempts, so heavy execution should continue to delegate.
  - Reflection Candidate: Hermes should own native durable memory and session recall, while Watson OM should stay limited to curated external markdown memory and hygiene.

## Retain (Observer Output)

## Reflection Candidates
