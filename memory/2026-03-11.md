# 2026-03-11

- User requested strict adherence to delegation-contract workflow for upcoming tasks and allowed reasonable infra tweaks.
- Prepared runtime for future passthrough delegation by adding `ollama/qwen3.5:9b` to OpenClaw fallback models.
- Kept primary default model unchanged; this was a non-disruptive availability/configuration step only.

- Timestamp: 2026-03-11T01:46:43Z
  - Event: Delegation-loop test run executed
  - Context: Validated runtime + OM scripts + model visibility
  - Source: reports/openclaw/delegation-loop-test-2026-03-11T01-45-58Z.md

- Timestamp: 2026-03-11T18:19:00Z
  - Event: Recipe and delegation readiness check completed
  - Context: Verified all WatsonOW recipes, confirmed delegation recipe is executable in current runtime, and validated context-window config behavior.
  - Details:
    - Recipes present: 00-watsonow-operating-system, 01-om-loop, 02-delegation-contracts, 03-skills-authoring, test-03-agentic-loop.
    - Delegation recipe has both a written contract recipe and a prior passing test artifact in reports/openclaw/delegation-loop-test-2026-03-11T01-45-58Z.md.
    - Current config hardcodes `qwen3.5:9b` to `contextWindow: 128000` only; no broader context-window override was found.
    - Current live session model is `gpt-5.4`, which is already using the highest available context among configured models.

- Timestamp: 2026-03-11T18:29:00Z
  - Event: First agentic workflow run executed
  - Context: Juan requested a delegation-style workflow covering GitHub repo access, repo organization, branch checkout, and automatic commits.
  - Details:
    - Verified GitHub access for account `ieverythng` with repo scope.
    - Cloned `ieverythng/itrader-azr` into `repos/itrader-azr`.
    - Checked out `Phase-B-Training` and confirmed tracking branch.
    - Added `/repos/` to `.gitignore` so future clones stay out of Watson-bot git status.
    - Updated long-term memory to make auto-commit the default workflow unless Juan asks for staging-only.

- Timestamp: 2026-03-11T18:41:00Z
  - Event: Pulled upstream harness update and reconciled policy drift
  - Context: Juan pushed a major upstream commit updating the WatsonOW harness and operational files.
  - Details:
    - Fetched and rebased local work on top of upstream commit `334ab93`.
    - Resolved a single rebase conflict in `OBSERVATIONS.md` by preserving both historical observation entries.
    - Reviewed updated harness files (`AGENTS.md`, `HEARTBEAT.md`, `TOOLS.md`, `REFLECTIONS.md`, recipes, skills).
    - Reconciled git methodology so stage-first remains the harness baseline while Juan's active auto-commit override remains explicit and current.

- Timestamp: 2026-03-11T18:58:00Z
  - Event: Pushed Watson branch and assessed `nao-ros4hri-bridge`
  - Context: Juan requested a push of the latest Watson commits plus a first-pass setup/dependency analysis for future NAO-ROS2 ablations.
  - Details:
    - Pushed `feat/Foundations_OM_Skills` to origin successfully.
    - Cloned `ieverythng/nao-ros4hri-bridge` into `repos/nao-ros4hri-bridge`.
    - Checked out `refactor/modularise-nodes-to-ROS4HRI-standard`.
    - Confirmed the repo is ROS 2 Jazzy + ROS4HRI + NAO + Vosk oriented.
    - Confirmed `Dockerfile.full` is the correct bootstrap path for a fresh machine, while `docker/Dockerfile` assumes a pre-existing `iiia:nao` base image.
    - Local blockers found: missing Docker/Podman, missing ROS Jazzy, missing colcon, missing pip, and no sudo in the current session.
    - Positive signals found: SocialMinds apt repo reachable, Ollama reachable, `/dev/snd` present.
    - Wrote feasibility report under `reports/openclaw/nao-ros4hri-bridge-local-feasibility-2026-03-11.md`.

- Timestamp: 2026-03-11T19:30:00Z
  - Event: Investigated gaming CPU bottleneck, Qwen tool instability, and Discord rollout path
  - Context: Juan requested a deep investigation into CPU bottlenecks for modern games (specifically Marathon), possible local-model cost-control patterns using Qwen3.5, and a future Discord setup plan.
  - Details:
    - Confirmed likely gaming pain points: Ryzen 5 5500 CPU ceiling, 16 GB RAM, background process noise, and C: drive nearly full.
    - Confirmed host power plan is Ultimate Performance, Game Mode enabled, Game DVR disabled, HAGS enabled.
    - Confirmed C: drive has only ~21.3 GB free out of ~446 GB; Steam on C: consumes ~119 GB and Marathon itself is ~19 GB.
    - Found likely root cause of Qwen/Ollama tool unreliability: OpenClaw config points Ollama to `/v1` OpenAI-compatible mode while docs recommend native Ollama API without `/v1` for tool calling.
    - Concluded best cost-control pattern is Qwen in a read-mostly planner lane and Codex as executor.
    - Wrote Discord rollout recommendation emphasizing DM pairing first, then private guild allowlist and structured channels.
    - Wrote consolidated report to `reports/openclaw/gaming-qwen-discord-investigation-2026-03-11.md`.

- Timestamp: 2026-03-11T20:00:00Z
  - Event: Added follow-up findings for bottleneck audit
  - Context: Juan asked specifically about Temp deletion safety, SSD vs HDD trade-offs, WSL vs Ubuntu, and Windows Defender CPU spikes.
  - Details:
    - Confirmed `AppData\\Local\\Temp` is ~4.0 GB and contents are generally safe to purge with locked files skipped.
    - Confirmed WSL Ubuntu is stored in a ~30 GB `ext4.vhdx` on C: and is distinct from any hypothetical standalone Ubuntu install.
    - Confirmed Defender is fully enabled, tamper-protected, and had a quick scan today; `MsMpEng` remains active.
    - Identified a large startup/background set including Ollama, Docker Desktop, launchers, Adobe sync, Autodesk, SOLIDWORKS helpers, and a suspicious `Web Companion` entry.

- Timestamp: 2026-03-11T23:02:41Z
  - Event: Verified OpenClaw↔Ollama localhost path and current Qwen visibility mismatch
  - Context: Juan asked to resume the prior machine/Qwen discussion and specifically wanted the localhost discovery steps.
  - Details:
    - Confirmed `http://127.0.0.1:11434/api/tags` and `http://localhost:11434/api/tags` are reachable from the OpenClaw/WSL side.
    - Confirmed the configured `http://172.24.16.1:11434/v1` endpoint times out and is stale/bad for current runtime.
    - Confirmed local `ollama list` currently exposes only `gpt-oss:20b-cloud`, not `qwen3.5:9b`.
    - Confirmed OpenClaw still advertises `ollama/qwen3.5:9b` only because of explicit manual config, so localhost discovery plus an actual Ollama Qwen install are both needed.
    - Evidence: `~/.openclaw/openclaw.json`, `~/.openclaw/agents/main/agent/models.json`, `openclaw status`, `openclaw models list`, `ollama list`, localhost `/api/tags` probe.

- Timestamp: 2026-03-11T23:06:50Z
  - Event: Confirmed dual-Ollama split between WSL and Windows installs
  - Context: Juan reported that Qwen appears in the Ollama app after restarting services and suspected an older client was still being used somewhere.
  - Details:
    - Confirmed WSL/OpenClaw currently talks to Linux Ollama at `/usr/local/bin/ollama`, version `0.15.4`, served by systemd unit `/etc/systemd/system/ollama.service` on `127.0.0.1:11434`.
    - Confirmed Windows Ollama exists at `C:\Users\Admin\AppData\Local\Programs\Ollama\ollama.exe`, version `0.17.7`.
    - Confirmed Windows Ollama data lives under `C:\Users\Admin\.ollama`, where the `qwen3.5:9b` manifest exists.
    - Confirmed the Windows Ollama server is reachable from WSL at `http://172.24.16.1:11434` and exposes `qwen3.5:9b`.
    - Conclusion: the earlier mismatch was caused by a local WSL Ollama daemon shadowing Windows localhost, not by Qwen being absent from the Windows app.

- Timestamp: 2026-03-11T23:28:36Z
  - Event: Verified native-endpoint Qwen tool calling and session labeling flow
  - Context: Juan asked how to operate OpenClaw sessions cleanly and whether Qwen still had broken tool wiring after the endpoint fix.
  - Details:
    - Confirmed `qwen3.5:9b` on the Windows Ollama `0.17.7` endpoint now performs a real `exec` tool call end-to-end in a throwaway session.
    - Evidence from transcript shows assistant `toolCall` → `toolResult` → final answer, not just schema imitation.
    - Confirmed session labels can be set with `sessions.patch` using `label`, which also surfaces as `displayName` in session lists.
    - Confirmed random `tui-*` session clutter comes from `/new`; named keys should be preferred via `/session <name>` for scratch lanes.
