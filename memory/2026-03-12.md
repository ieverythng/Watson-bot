- Timestamp: 2026-03-12T03:08:00+0100
  - Event: Runtime alignment audit rerun initiated per Recipe 04
  - Context: All operations delegated via sessions_spawn to dev; WatsonOW-Memory owns memory writes

- Timestamp: 2026-03-12T03:11:00+0100
  - Event: Logged runtime alignment gap: OpenClaw OM retrieval currently lacks explicit memory search paths.
  - Evidence: Active alignment findings indicate the retrieval path set is implicit/absent, so WatsonOW memory lanes are not guaranteed to be searched during OM lookups.
  - Fix plan: Patch the OpenClaw config to add explicit OM search targets for `MEMORY.md`, `OBSERVATIONS.md`, `REFLECTIONS.md`, and `memory/`, then rerun retrieval validation.
  - Reflection candidate: Retrieval-critical memory paths should be declared explicitly in config instead of relying on defaults.

- Timestamp: 2026-03-12T03:35:31+0100
  - Event: Added a deterministic-first OM retriever worker plus optional observer post-capture hook wiring.
  - Evidence: `reports/openclaw/retriever-script-draft-2026-03-12.md`, `scripts/om/retriever-worker.sh`, `scripts/om/observer-worker.sh`
  - Validation: `bash -n scripts/om/retriever-worker.sh scripts/om/observer-worker.sh`, `scripts/om/retriever-worker.sh retrieve --query "auto-commit override" --json`, isolated observer hook smoke test with scratch `OBS_FILE`/`MEM_DIR` under `reports/openclaw/`

- Timestamp: 2026-03-12T04:17:00+0100
  - Event: Audited Ollama/OpenClaw embedding prerequisites for `nomic-embed-text` setup using workspace evidence only.
  - Evidence: `OBSERVATIONS.md`, `reports/openclaw/mainpc-om-validation-2026-02-25T22-01-25Z.md`, `memory/2026-03-12.md`
  - Findings: Existing evidence shows OpenClaw memory search is enabled but still has no embedding provider; 2026-03-11 infra observations indicate the live Windows Ollama endpoint at `172.24.16.1:11434` held the real `qwen3.5:9b` model while WSL-local `127.0.0.1:11434` exposed a different model set. This subagent could not run fresh `ollama` or `openclaw` commands with the current toolset, so daemon status and installed-model state remain evidence-based rather than newly probed.
  - Next step: From a shell on the active Ollama host, run `ollama list`, pull `nomic-embed-text` there if absent, point OpenClaw memory search at that same local provider/model, and rerun `openclaw memory status --deep`.
  - Reflection candidate: When dual Ollama installations exist, embedding setup must target the same endpoint OpenClaw actually uses or memory indexing will appear configured but stay ineffective.
