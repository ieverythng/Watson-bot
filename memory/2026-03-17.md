- Timestamp: 2026-03-17T02:33:00+0100
  - Event: Moved oversized transcript artifact to `reports/openclaw/transcripts/2026-03-11-repo-memory-status.md` per OM hygiene rules. Daily memory now keeps only the report pointer and the recorded observation.
  - Evidence: `move artifact`, updated `memory/2026-03-17.md`
  - Evidence: `git status --short --branch`, `git pull --ff-only origin refactor/modularise-nodes-to-ROS4HRI-standard`, `git log --oneline efe2ba6..2f2eef6`, `git diff --stat efe2ba6..2f2eef6`
  - Findings: Pull brought in 12 commits focused on ROS4HRI migration cleanup, splitting legacy chatbot/dialogue-manager logic into dedicated skill/orchestrator packages (`nao_say_skill`, `nao_look_at`, `nao_replay_motion`, `nao_orchestrator`), Docker/docs refreshes, and removal of legacy monorepo entrypoints. Repo ended clean with no local modifications to stage.

- Timestamp: 2026-03-17T02:24:00+0100
  - Event: Audited OM lane health and promotion hygiene.
  - Evidence: `MEMORY.md`, `OBSERVATIONS.md`, `REFLECTIONS.md`, `memory/2026-03-11.md`, `reports/openclaw/transcripts/2026-03-11-repo-memory-status.md`, `memory/2026-03-17.md`, `reports/recipes/01-om-loop.md`
  - Findings: `nao-ros4hri-bridge` activity is present in daily audit memory and not bloating durable memory; `MEMORY.md` remains compact. Promoted the transcript-routing rule into `MEMORY.md` because a 459-line status dump under `memory/` shows the policy is durable and still operationally relevant.

- Timestamp: 2026-03-17T02:33:00Z
  - Event: Captured OM automation setup findings before compaction.
  - Evidence:
    - `openclaw cron add --help`
    - `~/.openclaw/agents/*/sessions/sessions.json`
    - `~/.openclaw/agents/*/sessions/*.jsonl`
    - `~/.openclaw/subagents/runs.json`
    - `~/.codex/auth.json`
  - Findings:
    - This OpenClaw build exposes gateway-backed cron management via `openclaw cron add`, and cron jobs can target non-default agents with `--agent <id>` plus isolated `agentTurn` payloads.
    - Codex token/cost accounting is available locally without extra API calls by parsing assistant `usage` blocks in OpenClaw session JSONL files and session metadata in `sessions.json`; this is suitable for a cheap cached expenditure tracker.
    - Active subagent state is available from `~/.openclaw/subagents/runs.json`, so a lightweight status CLI can report live delegate counts without polling the gateway.
    - `reports/openclaw/transcripts/2026-03-11-repo-memory-status.md` now holds the 459-line artifact; transcript relocation is complete and the memory lane no longer carries the oversized dump.

- Timestamp: 2026-03-17T03:18:00+0100
  - Event: Updated OpenClaw harness subagent concurrency in `~/.openclaw/openclaw.json` via `openclaw gateway call config.patch` and verified the gateway came back healthy.
  - Evidence: `openclaw gateway call config.get --json`, `openclaw gateway call config.patch --json`, `openclaw gateway status`, `/home/juanbeck/.openclaw/openclaw.json`
  - Findings: `agents.defaults.subagents.maxConcurrent` was implicit/effective `8` before the change and is now explicit `3`; gateway restart was applied by SIGUSR1 and post-restart parsed config reported `raw=3`, `resolved=3`, warnings `[]`. Staging the config file itself is blocked because `~/.openclaw` is not a git worktree.

- Timestamp: 2026-03-17T03:47:00+0100
  - Event: Ran the bootstrap OM memory sweep heartbeat.
  - Evidence: `python3 scripts/token_tracker.py report --date $(date -u +%F) --write`, `python3 scripts/token_tracker.py status --date $(date -u +%F)`, `reports/expenditure/ledger-2026-03-17.md`, `MEMORY.md`, `reports/openclaw/transcripts/2026-03-11-repo-memory-status.md`, `memory/2026-03-17.md`
  - Findings: Initial Codex-only expenditure snapshot for 2026-03-17 was written successfully. `MEMORY.md` remains compact at 51 lines. The stale/orphaned 459-line transcript/status dump identified in the sweep now lives at `reports/openclaw/transcripts/2026-03-11-repo-memory-status.md`; no other `memory/` file crossed the cleanup threshold in this sweep. A grep probe intended to pattern-scan transcript markers failed due to shell quoting, but the line-count review and file preview were sufficient for this heartbeat.
- Timestamp: 2026-03-17T02:48:00Z
  - Event: Completed OM automation wiring for cron, Codex expenditure tracking, and delegation-budget controls.
  - Evidence:
    - `openclaw cron add --name WatsonOM-MemorySweep ...`
    - `openclaw cron add --name WatsonOM-ImmediateMemorySweep ...`
    - `openclaw cron list --all --json`
    - `scripts/token_tracker.py`
    - `reports/expenditure/ledger-2026-03-17.md`
    - `reports/recipes/02-delegation-contracts.md`
  - Findings:
    - Registered two isolated `memory`-agent cron jobs: one hourly sweep and one one-shot bootstrap sweep on the next heartbeat.
    - Replaced the placeholder token tracker with a local Codex-only tracker that reads OpenClaw session usage, caches hourly metrics attempts, and exposes `status`, `report`, and `snapshot` commands.
    - Updated the 2026-03-17 expenditure ledger with a managed Codex token/cost summary, active subagent table, stale-memory flags, and smoothed/spike-risk projections to 2026-03-18.
    - Hardened delegation contracts with mandatory Codex token caps, cost-aware routing notes, and exception handling rules.
