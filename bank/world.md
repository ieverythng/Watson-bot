# World

Stable environment and infrastructure facts that affect decisions.

## Runtime Environment

- Repository: `Watson-bot`
- Canonical memory medium: markdown files in repo

## Platform Constraints

- Git workflow expectations may be overridden by direct user instruction; check `MEMORY.md`, `AGENTS.md`, and the current task before assuming stage-first or auto-commit.
- External/public actions require explicit confirmation.
- Memory system should stay auditable and provenance-backed.

## Deployment Host Snapshot

Fill on target runtime host (do not assume from non-target machines):

- `host.os`:
- `host.cpu`:
- `host.ram_gb`:
- `host.gpu`:
- `openclaw.available`:
