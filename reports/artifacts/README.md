# Artifacts

Scratch outputs and auxiliary generated files go here (temporary exports, intermediate dumps, ad-hoc logs, etc.).

Use this folder for **extra** generated material.
Measured/structured reports should remain in their domain folders (e.g., `reports/openclaw/`).
